# ShellGuard configuration — KEY=value, sourced by shellguard.sh
# Copy to /etc/shellguard/shellguard.conf

SHELLGUARD_HOST=            # empty → auto (hostname)
POLL_INTERVAL=2
DEDUP_WINDOW=60
LOG_FILE=/var/log/shellguard.log
LOG_STDOUT=1

TELEGRAM_ENABLED=0
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_TOPIC_ID=

SLACK_ENABLED=0
SLACK_WEBHOOK_URL=

WEBHOOK_ENABLED=0
WEBHOOK_URL=
WEBHOOK_SECRET=
