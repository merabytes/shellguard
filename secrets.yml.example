# ShellGuard secrets — NOT committed to git
# Copy from secrets.yml.example and fill in your values
# Used by: tests/e2e/run_e2e.sh (loaded automatically if present)

telegram:
  bot_token: ""       # @BotFather token
  chat_id: ""         # your chat or group ID
